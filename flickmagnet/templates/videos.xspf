<?xml version="1.0" encoding="UTF-8"?>
 <playlist version="1" xmlns="http://xspf.org/ns/0/">
    <trackList>
% for video in videos:
    <track>
      <title>${video['id']}</title>
      <location>${ url_base + 'torrents/' + video['hash'] + '/' + video['filename'] }</location>
    </track>
% endfor
    </trackList>
 </playlist>
